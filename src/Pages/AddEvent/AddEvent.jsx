import { Helmet } from "react-helmet-async";

const AddEvent = () => {
  return (
    <div>
      <Helmet>
        <title>Add Event</title>
      </Helmet>
      AddEvent
    </div>
  );
};

export default AddEvent;
